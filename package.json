{
    "name": "createPolygon-in-HTML5",
    "description": "",
    "version": "0.1.0",
    "author": "Fredrik Kemling",
    "scripts": {
        "tslint": "tslint --project .",
        "build": "tsc",
        "test": "nyc --reporter=html --reporter=text mocha --require ts-node/register --require source-map-support/register --exit test/**/*.ts",
        "coverage": "nyc report"
    },
    "devDependencies": {
        "@types/chai": "^4.2.11",
        "@types/mocha": "^7.0.2",
        "chai": "^4.2.0",
        "mocha": "^7.1.1",
        "nyc": "^15.0.1",
        "source-map-support": "^0.5.18",
        "ts-node": "^8.8.2",
        "typescript": "^3.8.3"
    }
}